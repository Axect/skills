# Secrets and local credentials
**/credentials.json
**/*.pem
**/*.key
**/*.p12
**/*.pfx
**/.env
**/.env.*

# Local scratch and scan outputs
*.log
*.tmp
*.out

# Skill run outputs (paperbanana generations, adversarial-review reports,
# research-report artifacts) — intentionally local.
outputs/

# Failure archive is intentionally tracked in history — see commit-triage skill.
!failure/
